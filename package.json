{
  "name": "sa-raichur-service-point-workspace",
  "version": "1.0.0",
  "description": "Full stack home services marketplace for S A Raichur Service Point",
  "main": "server/server.js",
  "scripts": {
    "install-all": "npm install && npm install --prefix server && npm install --production=false --prefix client",
    "server": "npm start --prefix server",
    "client": "npm run dev --prefix client",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "build-client": "npm run build --prefix client"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "author": "Antigravity",
  "license": "ISC"
}
